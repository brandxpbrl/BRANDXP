export type HeroCta = {
  label: string;
  href: string;
  external?: boolean;
};

export type HeroBadge = {
  label: string;
};

export type HeroFeature = {
  label: string;
};

export type HeroMedia = {
  src: string;
  alt: string;
  width: number;
  height: number;
};
