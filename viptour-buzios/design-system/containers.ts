/**
 * Design Token: Containers
 * Anchos máximos para contenedores, alineados con la jerarquía visual.
 */
export const containers = {
  narrow: 'var(--container-narrow)',
  default: 'var(--container-default)', // Container normal
  wide: 'var(--container-wide)',
  ultraWide: 'var(--container-ultra-wide)',
  full: '100%',
} as const;
